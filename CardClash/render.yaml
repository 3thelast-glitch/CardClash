services:
  - type: web
    name: card-clash-multiplayer
    runtime: node
    plan: free
    buildCommand: corepack enable && pnpm install --frozen-lockfile
    startCommand: pnpm server:multiplayer
    healthCheckPath: /health
    envVars:
      - key: MULTIPLAYER_PORT
        value: $PORT
